# EmailJS configuration for the contact form.
# Sign up at https://www.emailjs.com, create a service + template, then fill these in.
# Without them the contact form gracefully falls back to opening the user's mail client.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=
